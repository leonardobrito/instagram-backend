describe("PostController", () => {
  it("Shold list all Posts", () => {
    const post = true;
    expect(post).toBe(true);
  });

  it("Shold create a new Post", () => {
    const post = true;
    expect(post).toBe(true);
  });
});
